- name: Setup dircolors
  template:
    src: "dir_colors"
    dest: "~/.dir_colors"

- name: Setup vim configuration
  template:
    src: "vimrc"
    dest: "~/.vimrc"

- name: Setup ZSH configuration
  template:
    src: "zshrc"
    dest: "~/.zshrc"

- name: Setup Git VCS
  include_tasks: git.yaml
